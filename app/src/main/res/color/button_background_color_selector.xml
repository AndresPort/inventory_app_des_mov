<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        Estado deshabilitado: android:state_enabled="false"
        Usamos el mismo color para que no cambie.
    -->
    <item android:state_enabled="false" android:color="@color/buttonColor" />

    <!--
        Estado por defecto (habilitado)
        Este debe ir al final.
    -->
    <item android:color="@color/buttonColor" />
</selector>
